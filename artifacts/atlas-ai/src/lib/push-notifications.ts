import {notificationPermission} from "./notifications";const DEVICE_KEY="atlas_push_device_id_v1";
const device=()=>{if(typeof window==="undefined")return"";const x=localStorage.getItem(DEVICE_KEY);if(x)return x;const id=crypto.randomUUID();localStorage.setItem(DEVICE_KEY,id);return id;};
export async function registerServiceWorker(){if(!("serviceWorker"in navigator))return null;try{const r=await navigator.serviceWorker.register("/sw.js",{scope:"/"});await navigator.serviceWorker.ready;return r;}catch{return null;}}
const decode=(v:string)=>{const raw=atob(v+"=".repeat((4-v.length%4)%4).replace(/-/g,"+").replace(/_/g,"/"));return Uint8Array.from(raw,c=>c.charCodeAt(0));};
export async function subscribeToPush(){const r=await registerServiceWorker();if(!r||!("PushManager"in window)||notificationPermission()!=="granted")return null;try{let sub=await r.pushManager.getSubscription();const k=await fetch("/api/notifications/push/public-key");if(!k.ok)return null;const {publicKey}=await k.json() as {publicKey:string};if(!sub)sub=await r.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:decode(publicKey)});const saved=await fetch("/api/notifications/push/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({deviceId:device(),subscription:sub.toJSON()})});return saved.ok?sub:null;}catch{return null;}}
export async function schedulePushNotification(input:{id:string;kind:"task"|"reminder"|"life-rescue";scheduledAt:string;title:string;body:string;url?:string}){if(!device()||!input.scheduledAt)return false;try{const r=await fetch("/api/notifications/push/schedule",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({deviceId:device(),id:input.id,kind:input.kind,scheduledAt:input.scheduledAt,payload:{title:input.title,body:input.body,url:input.url??"/izci",tag:`atlas-${input.kind}-${input.id}`,renotify:true,actions:[{action:"open",title:"Aç"}]}})});return r.ok;}catch{return false;}}